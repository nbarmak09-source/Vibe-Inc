# Copy to ".env.local" — Next.js does NOT load this file; only .env.local (and .env) are used at runtime.

ACCESS_CODES=WVIBE2026,QVIBE2026
ACCESS_SESSION_SECRET=replace-with-a-long-random-secret-use-openssl-rand-base64-32
